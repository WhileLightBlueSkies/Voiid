Latin subsets of Plus Jakarta Sans, Geist and Geist Mono (variable, all weights), the same
faces the admin console uses. Self-hosted because the CSP allows `font-src 'self'` only.
All three are licensed under the SIL Open Font License 1.1.

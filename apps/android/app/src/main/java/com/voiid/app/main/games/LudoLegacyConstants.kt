package com.voiid.app.main.games

import androidx.compose.ui.graphics.Color

/** Compatibility constants for generic match-history formatting; not a rules or bot path. */
object Ludo {
    const val HOME = 200
    val SEAT_COLORS = listOf(
        Color(0xFFCF514F), Color(0xFF5F9F5E), Color(0xFFE9BD2E), Color(0xFF4B78E5),
    )
}

<resources xmlns:tools="http://schemas.android.com/tools">
    <!--
      VOIID is a fixed light design (background #DFDFDF in both light & dark).
      Compose draws the whole UI; this theme only provides the window background
      (to avoid a white launch flash) + light system-bar icons. NoActionBar.
    -->
    <style name="Theme.Voiid" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/voiid_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">true</item>
    </style>
</resources>

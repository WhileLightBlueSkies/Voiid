<resources xmlns:tools="http://schemas.android.com/tools">
    <!--
      LIGHT launch window. Compose draws the whole UI; this theme only supplies the window
      background (so a cold start does not flash white) and dark system-bar icons for the
      warm off-white ground. The dark counterpart is values-night/themes.xml.
    -->
    <style name="Theme.Voiid" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/voiid_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">true</item>
    </style>
</resources>

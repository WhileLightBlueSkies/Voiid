# Rust build artifacts — never commit these (hundreds of MB).
/target
/fuzz/target

# Generated binding build outputs (regenerated by build-apple.sh / build-android.sh).
/target/apple

# macOS
.DS_Store

# Cargo.lock IS committed on purpose: it pins exact (audited) crypto dependency
# versions for reproducible builds. Do not add it here.
